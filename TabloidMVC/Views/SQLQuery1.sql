SELECT id, name FROM Category
ORDER BY name

SELECT c.Id, c.name
                        FROM Category c

                        SELECT Id, name
                        FROM Category 
                        